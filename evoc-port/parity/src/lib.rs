//! Empty by design: this crate exists to host the parity tests and benchmark
//! examples for `evoc-core` (see `tests/` and `examples/`), keeping their
//! serde_json dependency out of the crate that ships inside the R package.
