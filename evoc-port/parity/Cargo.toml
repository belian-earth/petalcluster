[package]
name = "evoc-parity"
version = "0.0.1"
edition = "2021"
rust-version = "1.81"
publish = false
description = "Parity tests and benchmarks for evoc-core against the fixtures in ../fixtures. Dev-only: this crate is .Rbuildignored and never ships."
license = "Apache-2.0 AND BSD-2-Clause"

[dependencies]
evoc-core = { path = "../../src/rust/evoc-core" }
serde_json = "1"
