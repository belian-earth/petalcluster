% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threads.R
\name{shoal_threads}
\alias{shoal_threads}
\title{Threads used by the Rust backends}
\usage{
shoal_threads(n)
}
\arguments{
\item{n}{Number of threads, a positive whole number. Omit to query.}
}
\value{
The number of threads in the pool, invisibly when setting.
}
\description{
The parallel parts of the package run on a thread pool owned by it.
\code{shoal_threads(n)} rebuilds that pool with \code{n} threads; \code{shoal_threads()}
reports the current size. The setting takes effect immediately and lasts
for the session.
}
\details{
Parallel work covers \code{\link[=shoal_evoc]{shoal_evoc()}}'s neighbour search, spanning tree and
node embedding; \code{\link[=shoal_dbscan]{shoal_dbscan()}}'s neighbour queries and
\code{\link[=shoal_hdbscan]{shoal_hdbscan()}}'s core distances and spanning tree; \code{\link[=shoal_kmeans]{shoal_kmeans()}}'s
assignment step and initialisation, which the Gaussian mixture shares
through its k-means start; \code{\link[=shoal_dist]{shoal_dist()}}; and \code{\link[=shoal_silhouette]{shoal_silhouette()}}.
Hierarchical clustering and the mixture's EM iterations are
single-threaded. Results never depend on the thread count.
}
\section{Default}{
On load the pool takes, in order of precedence, the \code{shoal.threads} option,
the \code{RAYON_NUM_THREADS} environment variable, or one thread per logical
core. When \verb{_R_CHECK_LIMIT_CORES_} is set, as it is by
\verb{R CMD check --as-cran}, the automatic default is capped at 2 to respect
the CRAN policy for checks; an explicit option or variable is still
honoured.
}

\examples{
old <- shoal_threads()
shoal_threads(2)
shoal_threads()
shoal_threads(old)

}
