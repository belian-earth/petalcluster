% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hclust.R
\name{shoal_hclust}
\alias{shoal_hclust}
\title{Hierarchical Agglomerative Clustering}
\usage{
shoal_hclust(
  d,
  method = c("complete", "single", "average", "weighted", "ward", "centroid", "median")
)
}
\arguments{
\item{d}{A \link[stats:dist]{stats::dist} object, or a numeric matrix or data frame, in which
case Euclidean distances are computed on the way, into the single buffer
the clustering works on: half the peak memory of calling \code{\link[=shoal_dist]{shoal_dist()}}
first, at the cost of not keeping the distances.}

\item{method}{Linkage method. One of \code{"complete"}, \code{"single"}, \code{"average"},
\code{"weighted"}, \code{"ward"}, \code{"centroid"} or \code{"median"}.}
}
\value{
An object of class \code{"hclust"} with components \code{merge}, \code{height},
\code{order}, \code{labels}, \code{method}, \code{call} and \code{dist.method}.
}
\description{
Agglomerative clustering via the Rust
\href{https://github.com/diffeo/kodama}{kodama} crate, a port of
\emph{fastcluster}. Returns a standard \link[stats:hclust]{stats::hclust} object, so
\code{\link[stats:cutree]{stats::cutree()}}, \code{\link[stats:as.dendrogram]{stats::as.dendrogram()}}, \code{plot()} and the rest of R's
hierarchical clustering ecosystem work on the result unchanged.
}
\section{Linkage methods}{
Dissimilarities are squared internally for \code{"ward"}, \code{"centroid"} and
\code{"median"}, with the square root taken afterwards, the fastcluster and SciPy
convention. Two consequences differ from \code{\link[stats:hclust]{stats::hclust()}}:
\itemize{
\item \code{"ward"} corresponds to R's \code{"ward.D2"}, not \code{"ward.D"}.
\item \code{"centroid"} and \code{"median"} take plain distances here, whereas
\code{\link[stats:hclust]{stats::hclust()}} expects them to be squared beforehand.
}

\code{"weighted"} is R's \code{"mcquitty"} (WPGMA); \code{"average"} is UPGMA.

\code{"centroid"} and \code{"median"} can produce inversions, a merge at a lower
height than the one before it. This is a property of the methods, not a bug,
but \code{\link[stats:cutree]{stats::cutree()}} rejects such trees, so a warning is issued when it
happens.
}

\examples{
d <- shoal_dist(as.matrix(iris[, 1:4]))
fit <- shoal_hclust(d, method = "ward")
cutree(fit, k = 3)

}
\seealso{
\code{\link[=shoal_dist]{shoal_dist()}} for building the distance matrix.
}
