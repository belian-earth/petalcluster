% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kmeans.R
\name{shoal_kmeans}
\alias{shoal_kmeans}
\title{K-Means Clustering}
\usage{
shoal_kmeans(
  x,
  k,
  init = c("kmeans++", "kmeans_parallel", "random"),
  n_runs = 10L,
  max_iter = 300L,
  tolerance = 1e-04,
  seed = 1L
)
}
\arguments{
\item{x}{A numeric matrix or data frame. Data frames are coerced to a matrix
using their numeric columns (non-numeric columns are dropped).}

\item{k}{Number of clusters. Required — there is no sensible default for
the central modelling decision. Must be at least 1 and no more than \code{nrow(x)}.}

\item{init}{Initialisation method. \code{"kmeans++"} (default) is the usual
choice; \code{"kmeans_parallel"} scales better past roughly 100 clusters;
\code{"random"} is the naive baseline.}

\item{n_runs}{Number of restarts, keeping the fit with the lowest inertia.
Default \code{10L}.}

\item{max_iter}{Maximum iterations per run. Default \code{300L}.}

\item{tolerance}{Convergence threshold on centroid movement. Default \code{1e-4}.}

\item{seed}{Non-negative whole-number seed for initialisation. Stored and
passed as a double, so values beyond the integer range are safe. Default \code{1L}.}
}
\value{
An object of class \code{c("shoal_kmeans", "shoal_clustering")}: a list
with components \code{cluster} (integer vector of cluster IDs), \code{n_clusters},
\code{n_noise} (always \code{0}), \code{data}, \code{algorithm}, \code{params}, \code{centroids}
(a \verb{k x ncol(x)} matrix), \code{inertia} and \code{sizes}.
}
\description{
Lloyd's algorithm via the Rust
\href{https://github.com/rust-ml/linfa}{linfa} toolkit, with k-means++ and
k-means|| initialisation.
}
\details{
Unlike the density-based algorithms, k-means partitions every observation —
there is no noise class, so \code{cluster} never contains \code{NA}. It is also
predictive: \code{\link[=predict]{predict()}} assigns new observations to the fitted centroids.
}
\section{Reproducibility}{
k-means is stochastic in its initialisation, so \code{seed} is a parameter rather
than being taken from R's RNG. The same \code{seed} and parameters always give the
same partition; \code{set.seed()} has no effect on it.
}

\section{Choosing k}{
\code{inertia} (the within-cluster sum of squares) is returned so it can be
compared across values of \code{k} — the usual scree or elbow approach. Note that
it decreases monotonically with \code{k} by construction, so it identifies a
diminishing-returns point rather than an optimum.
}

\examples{
fit <- shoal_kmeans(as.matrix(iris[, 1:4]), k = 3L)
fit
fit$centroids

}
\seealso{
\code{\link[=predict.shoal_kmeans]{predict.shoal_kmeans()}} for assigning new observations.
}
