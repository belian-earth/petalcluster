% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{shoal_metrics}
\alias{shoal_metrics}
\title{Internal Cluster Validity Indices}
\usage{
shoal_metrics(x, cluster = NULL)
}
\arguments{
\item{x}{A numeric matrix or data frame, or a clustering result from this
package, in which case both the data and the assignment are taken from it.}

\item{cluster}{A clustering result or an integer vector of cluster IDs.
Required unless \code{x} is itself a clustering result. \code{NA} entries are
excluded.}
}
\value{
A one-row data frame with columns \code{n}, \code{k}, \code{calinski_harabasz} and
\code{davies_bouldin}.
}
\description{
Computes the Calinski-Harabasz and Davies-Bouldin indices, both of which
work from the data rather than a distance matrix, and both of which are used
the same way: fit at several values of \code{k} and compare.
}
\details{
\itemize{
\item \strong{Calinski-Harabasz} is between-cluster dispersion over within-cluster
dispersion, each per degree of freedom. \strong{Higher is better.}
\item \strong{Davies-Bouldin} averages, over clusters, the worst-case ratio of
combined within-cluster scatter to the distance between centroids.
\strong{Lower is better.}
}

Both assume roughly convex, centroid-shaped clusters, so they suit
\code{\link[=shoal_kmeans]{shoal_kmeans()}} and \code{\link[=shoal_gmm]{shoal_gmm()}} better than the density-based algorithms.
For arbitrary cluster shapes prefer \code{\link[=shoal_silhouette]{shoal_silhouette()}}, which needs only a
distance matrix.
}
\examples{
fit <- shoal_kmeans(as.matrix(iris[, 1:4]), k = 3L)
shoal_metrics(fit)

# Comparing candidate values of k
do.call(rbind, lapply(2:5, function(k) {
  shoal_metrics(shoal_kmeans(as.matrix(iris[, 1:4]), k = k))
}))

}
\seealso{
\code{\link[=shoal_silhouette]{shoal_silhouette()}}.
}
