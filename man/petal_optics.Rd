% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optics.R
\name{petal_optics}
\alias{petal_optics}
\title{OPTICS Clustering}
\usage{
petal_optics(x, eps = 0.5, min_samples = 5L, metric = c("euclidean", "cosine"))
}
\arguments{
\item{x}{A numeric matrix or data frame. Data frames are coerced to a matrix
using their numeric columns (non-numeric columns are dropped).}

\item{eps}{Neighbourhood radius. Default \code{0.5}.}

\item{min_samples}{Minimum number of points to form a dense region. Default \code{5L}.}

\item{metric}{Distance metric, one of \code{"euclidean"} or \code{"cosine"}.}
}
\value{
An object of class \code{"petal_optics"}: a list with components
\code{cluster} (integer vector of cluster IDs, \code{NA} for noise),
\code{n_clusters}, \code{n_noise}, \code{data} (the input matrix), \code{params}, and \code{metric}.
}
\description{
Ordering points to identify the clustering structure.
}
