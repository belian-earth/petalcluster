% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm.R
\name{shoal_gmm}
\alias{shoal_gmm}
\title{Gaussian Mixture Model}
\usage{
shoal_gmm(
  x,
  k,
  init = c("kmeans", "random"),
  n_runs = 1L,
  max_iter = 100L,
  tolerance = 0.001,
  reg_covariance = 1e-06,
  seed = 1L
)
}
\arguments{
\item{x}{A numeric matrix or data frame. Data frames are coerced to a matrix
using their numeric columns (non-numeric columns are dropped).}

\item{k}{Number of mixture components. Required — there is no sensible
default for the central modelling decision.}

\item{init}{Initialisation method: \code{"kmeans"} (default) or \code{"random"}.}

\item{n_runs}{Number of restarts, keeping the best fit. Default \code{1L}:
unlike \code{\link[=shoal_kmeans]{shoal_kmeans()}}'s 10, a single run is the norm for EM (sklearn does
the same) because the k-means initialisation already starts close and each
run is expensive. Raise it for small, multimodal problems.}

\item{max_iter}{Maximum EM iterations per run. Default \code{100L}.}

\item{tolerance}{Convergence threshold on the log-likelihood. Default \code{1e-3}.}

\item{reg_covariance}{Value added to the diagonal of each covariance matrix
to keep it positive definite. Default \code{1e-6}.}

\item{seed}{Non-negative whole-number seed for initialisation. Stored and
passed as a double, so values beyond the integer range are safe. Default \code{1L}.}
}
\value{
An object of class \code{c("shoal_gmm", "shoal_clustering")}: a list with
components \code{cluster}, \code{n_clusters}, \code{n_noise} (always \code{0}), \code{data},
\code{algorithm}, \code{params}, \code{posterior} (an \verb{n x k} matrix of responsibilities),
\code{weights}, \code{means}, \code{covariances} (a \verb{k x p x p} array) and \code{loglik}.
}
\description{
Fits a mixture of multivariate Gaussians by expectation-maximisation, via the
Rust \href{https://github.com/rust-ml/linfa}{linfa} toolkit.
}
\details{
Unlike the other algorithms here, a GMM is generative: it gives each
observation a probability of belonging to each component rather than a single
label. \code{posterior} holds those responsibilities; \code{cluster} is their row-wise
maximum, provided for consistency with the rest of the package.

Clusters are elliptical rather than spherical, so a GMM handles correlated
and differently-scaled features that k-means would split badly.
}
\section{Covariance structure}{
Only full covariance matrices are supported — each component gets its own
unconstrained matrix. The constrained families \code{mclust} offers (spherical,
diagonal, tied) are not available upstream.
}

\section{Choosing k}{
A \code{logLik()} method is provided, so \code{\link[stats:AIC]{stats::AIC()}} and \code{\link[stats:BIC]{stats::BIC()}} work
directly on a fitted model and can be compared across \code{k}. Unlike the
within-cluster sum of squares reported by \code{\link[=shoal_kmeans]{shoal_kmeans()}}, these penalise
parameter count and so have an interior optimum.
}

\examples{
fit <- shoal_gmm(as.matrix(iris[, 1:4]), k = 3L)
fit
BIC(fit)
head(fit$posterior)

}
\seealso{
\code{\link[=predict.shoal_gmm]{predict.shoal_gmm()}}, \code{\link[=logLik.shoal_gmm]{logLik.shoal_gmm()}}.
}
