% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdbscan.R
\name{shoal_hdbscan}
\alias{shoal_hdbscan}
\title{HDBSCAN Clustering}
\usage{
shoal_hdbscan(
  x,
  alpha = 1,
  min_samples = 15L,
  min_cluster_size = 15L,
  metric = c("euclidean", "cosine"),
  boruvka = TRUE,
  partial_labels = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame. Data frames are coerced to a matrix
using their numeric columns (non-numeric columns are dropped).}

\item{alpha}{Smoothing parameter for mutual reachability distance. Default \code{1.0}.}

\item{min_samples}{Minimum neighbourhood size. Default \code{15L}.}

\item{min_cluster_size}{Minimum cluster size. Default \code{15L}.}

\item{metric}{Distance metric, one of \code{"euclidean"} or \code{"cosine"}.}

\item{boruvka}{Whether to build the minimum spanning tree with a
tree-accelerated Boruvka search rather than Prim's algorithm. Default
\code{TRUE}, which is the faster choice in low dimensions. The acceleration
depends on a spatial index, and above a few dozen columns it becomes
slower than the plain search: on 20,000 rows in 64 dimensions Boruvka
takes around 2.5 times as long as \code{boruvka = FALSE}. Both give the same
clustering.}

\item{partial_labels}{Optional named list for semi-supervised clustering.
Names are cluster IDs (as strings), values are integer vectors of 1-indexed
point indices. \code{NULL} (default) for fully unsupervised clustering.}
}
\value{
An object of class \code{c("shoal_hdbscan", "shoal_clustering")}: a list
with components \code{cluster} (integer vector of cluster IDs, \code{NA} for noise),
\code{n_clusters}, \code{n_noise}, \code{data} (the input matrix), \code{algorithm}, \code{params},
\code{metric}, and \code{outlier_scores} (GLOSH scores).
}
\description{
Hierarchical density-based spatial clustering of applications with noise.
}
\examples{
res <- shoal_hdbscan(as.matrix(iris[, 1:4]))
res

}
