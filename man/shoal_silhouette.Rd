% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{shoal_silhouette}
\alias{shoal_silhouette}
\title{Silhouette Widths}
\usage{
shoal_silhouette(d, cluster)
}
\arguments{
\item{d}{A \link[stats:dist]{stats::dist} object, or a numeric matrix or data frame, in which
case Euclidean distances are computed with \code{\link[=shoal_dist]{shoal_dist()}} first.}

\item{cluster}{A clustering result from this package, or an integer vector of
cluster IDs. \code{NA} entries — noise points from the density-based algorithms
— are excluded, along with their distances.}
}
\value{
A data frame with one row per observation and columns \code{cluster},
\code{neighbour} (the nearest other cluster) and \code{width}. The mean width is
attached as the \code{avg_width} attribute.
}
\description{
For each observation, compares the mean distance to the rest of its own
cluster (\code{a}) with the smallest mean distance to any other cluster (\code{b}),
giving a width of \code{(b - a) / max(a, b)}. Widths near 1 indicate a
well-placed observation, near 0 one on a boundary, and negative ones an
observation that sits closer to a different cluster.
}
\details{
An observation alone in its cluster is given a width of 0 by convention,
since it has no within-cluster distances to average.
}
\examples{
x <- as.matrix(iris[, 1:4])
fit <- shoal_kmeans(x, k = 3L)
sil <- shoal_silhouette(shoal_dist(x), fit)
attr(sil, "avg_width")

}
\seealso{
\code{\link[=shoal_metrics]{shoal_metrics()}} for indices computed from the data instead.
}
