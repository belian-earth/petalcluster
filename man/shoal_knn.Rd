% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knn.R
\name{shoal_knn}
\alias{shoal_knn}
\alias{print.shoal_knn}
\alias{plot.shoal_knn}
\title{k-Nearest Neighbours}
\usage{
shoal_knn(
  x,
  k,
  query = NULL,
  metric = c("euclidean", "maximum", "manhattan", "canberra", "binary", "minkowski",
    "cosine", "correlation", "mahalanobis"),
  p = 2,
  cov = NULL,
  search = c("auto", "kdtree", "brute")
)

\method{print}{shoal_knn}(x, ...)

\method{plot}{shoal_knn}(x, which = x$k, ...)
}
\arguments{
\item{x}{A numeric matrix or data frame of the points to search among.
Data frames are coerced to a matrix using their numeric columns
(non-numeric columns are dropped). Rows containing missing or non-finite
values are an error.}

\item{k}{Number of neighbours to find. Must be less than \code{nrow(x)} when
\code{query} is \code{NULL}, and at most \code{nrow(x)} otherwise.}

\item{query}{Optional numeric matrix or data frame of points to find
neighbours for, with the same columns as \code{x}. \code{NULL} (default) searches
the rows of \code{x} against one another.}

\item{metric}{Distance metric. One of \code{"euclidean"}, \code{"maximum"},
\code{"manhattan"}, \code{"canberra"}, \code{"binary"}, \code{"minkowski"}, \code{"cosine"},
\code{"correlation"} or \code{"mahalanobis"}.}

\item{p}{Power for \code{metric = "minkowski"}. Ignored otherwise. Default \code{2}.}

\item{cov}{Covariance matrix for \code{metric = "mahalanobis"}, \code{ncol(x)} square
and positive definite. \code{NULL} (default) uses \code{\link[stats:cov]{stats::cov()}} of \code{x}, the
reference set, whichever of \code{x} and \code{query} is being measured. Ignored
for other metrics.}

\item{search}{Search algorithm: \code{"auto"} (default), \code{"kdtree"} or
\code{"brute"}. See Details. \code{"kdtree"} is an error for a metric the tree
cannot bound.}

\item{...}{For \code{plot()}, further arguments to \code{\link[=plot.default]{plot.default()}}; \code{main},
\code{xlab} and \code{ylab} given here replace the defaults. Ignored by \code{print()}.}

\item{which}{Which neighbour's distance to plot, between \code{1} and \code{x$k}.
Default \code{x$k}.}
}
\value{
An object of class \code{"shoal_knn"}: a list with components \code{id}, an
integer matrix of row indices into \code{x}, and \code{dist}, a numeric matrix of
the corresponding distances. Both have one row per point searched for
and \code{k} columns, nearest first, with the row names of \code{x} or \code{query} and
column names \code{1} to \code{k}. \code{k}, \code{metric} and \code{search} record the call,
\code{search} being the algorithm actually used.
}
\description{
Finds the \code{k} nearest neighbours of every row of \code{x} among the other rows,
or of every row of \code{query} among the rows of \code{x}, by an exact search in
Rust. Every metric of \code{\link[=shoal_dist]{shoal_dist()}} is available and the distances agree
with it exactly, but only the \code{k} nearest are kept, so the memory is
proportional to \code{k} times the number of rows rather than to the square of
it. Where a distance matrix stops being possible somewhere in the tens of
thousands of rows, a neighbour search does not.
}
\details{
Two exact searches are available and give identical results, tie order
included. \code{"brute"} compares every query row with every data row, in
parallel over queries; it serves every metric. \code{"kdtree"} builds an
axis-aligned kd-tree and skips regions that cannot hold a nearer point;
it serves every metric but \code{"canberra"} and \code{"binary"}, whose distances
no rectangle can bound. \code{"cosine"} and \code{"correlation"} are searched
through a unit-normalised (and, for correlation, centred) copy of the
rows, on which Euclidean distance orders points exactly as the metric
does; the distances reported are still the metric itself. A tree prunes
well in a few dimensions and hardly at all beyond about ten, where the
scan is several times faster than the kd-tree searches in the dbscan and
FNN packages even on one thread. The default \code{"auto"} takes the tree when
the metric allows it and \code{ncol(x)} is at most 8, and the scan otherwise.
The tree is built in parallel, so on a large low-dimensional set the
build is a small part of the total. The tree path holds one extra copy
of \code{x}, its rows reordered so that each leaf is contiguous in memory.

The conventions follow \code{dbscan::kNN()}, so code written for its results
works on these. Without \code{query}, each row is excluded from its own
neighbours. With \code{query}, every row of \code{x} is a candidate, so a query
identical to a data row finds that row at distance zero. Neighbours are
sorted by distance, and equal distances by row index, so the result is
fully determined by the input. Rows with missing or non-finite values are
an error rather than being dropped as \code{\link[=shoal_dist]{shoal_dist()}} does: dropping rows
would renumber the indices so they no longer point into the caller's
matrix.

The \code{plot()} method draws each point's distance to its \code{k}-th neighbour in
increasing order. The knee of that curve is the usual choice of \code{eps} for
\code{\link[=shoal_dbscan]{shoal_dbscan()}} with \code{min_samples = k + 1}; the point itself counts
towards \code{min_samples}, hence the one.
}
\examples{
x <- as.matrix(iris[, 1:4])
nn <- shoal_knn(x, k = 5L)
nn
head(nn$id)

# The distance to the fifth neighbour, sorted: read eps off the knee.
plot(nn)

# Neighbours of new points among the rows of x, by cosine distance.
shoal_knn(x, k = 3L, query = x[c(1, 51, 101), ], metric = "cosine")

}
\seealso{
\code{\link[=shoal_dist]{shoal_dist()}} for the full distance matrix, and
\code{\link[=shoal_dbscan]{shoal_dbscan()}}, whose \code{eps} the \code{plot()} method helps to choose.
}
