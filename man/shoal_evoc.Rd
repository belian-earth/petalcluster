% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evoc.R
\name{shoal_evoc}
\alias{shoal_evoc}
\title{EVoC: Embedding Vector Oriented Clustering}
\usage{
shoal_evoc(
  x,
  n_neighbors = 15L,
  noise_level = 0.5,
  min_cluster_size = 5L,
  min_samples = 5L,
  n_epochs = 50L,
  dim = NULL,
  max_layers = 10L,
  layer = "auto",
  seed = 1L
)
}
\arguments{
\item{x}{A numeric matrix or data frame of embedding vectors, one per row.
Data frames are coerced to a matrix using their numeric columns
(non-numeric columns are dropped). Must have more rows than \code{n_neighbors}.}

\item{n_neighbors}{Neighbourhood size for the nearest-neighbour graph.
Default \code{15L}.}

\item{noise_level}{Tolerance for spreading points out in the learned
embedding; higher values let clusters absorb more of their surroundings.
Default \code{0.5}.}

\item{min_cluster_size}{Minimum cluster size at the finest layer; at
least 2. Default \code{5L}, matching upstream, which is calibrated for large inputs;
on collections of a few thousand rows or fewer it tends to
over-fragment, and something like \code{15L} recovers the structure far more
reliably.}

\item{min_samples}{Minimum neighbourhood size for the density estimation.
Default \code{5L}.}

\item{n_epochs}{Training epochs for the node embedding. Default \code{50L}.}

\item{dim}{Dimension of the learned node embedding. \code{NULL} (default) uses
the upstream rule \code{min(max(n_neighbors / 4, 4), 15)}.}

\item{max_layers}{Maximum number of cluster layers to return. Default \code{10L}.}

\item{layer}{Which layer populates \code{cluster}: \code{"auto"} (default) selects the
layer with the highest persistence score, matching upstream behaviour; an
integer selects that layer directly (1 is the finest).}

\item{seed}{Non-negative whole-number seed. Stored and passed as a double,
so values beyond the integer range are safe. Default \code{1L}.}
}
\value{
An object of class \code{c("shoal_evoc", "shoal_clustering")}: a list
with components \code{cluster} (integer vector for the selected layer, \code{NA}
for noise), \code{n_clusters}, \code{n_noise}, \code{data}, \code{algorithm}, \code{params},
\code{metric} (always \code{"cosine"}), and the multi-layer results: \code{layers}
(list of integer cluster vectors, finest first), \code{strengths} (list of
numeric membership-strength vectors, aligned with \code{layers}),
\code{persistence} (numeric persistence score per layer), \code{layer} (index of
the selected layer) and \code{embedding} (the learned node-embedding matrix).
}
\description{
Direct multi-granularity clustering of embedding vectors, via an in-tree
Rust port of McInnes's
\href{https://github.com/TutteInstitute/evoc}{EVoC} (Tutte Institute).
Instead of clustering in the original space, EVoC builds a nearest-neighbour
graph under cosine distance, learns a compact node embedding from it, and
density-clusters that embedding at several granularities at once. On
collections of pre-built embeddings it is orders of magnitude faster than
running HDBSCAN on the raw vectors, at comparable quality.
}
\section{Domain}{
EVoC assumes cosine geometry: rows are L2-normalised internally and treated
as directions, which is the right model for text/image embedding vectors and
the wrong one for general tabular data. It also wants scale: behaviour is
calibrated for thousands to millions of rows. On small or low-dimensional
data it over-fragments and marks much of the input as noise (the reference
implementation behaves the same way); reach for \code{\link[=shoal_hdbscan]{shoal_hdbscan()}} there.
}

\section{Layers}{
A single flat clustering hides a genuine modelling choice, and the upstream
heuristic for making it (most persistent layer) is not reliable on every
shape of data. Every layer is therefore returned, finest first, along with
its persistence score and per-point membership strengths; \code{layer} only
chooses which one populates \code{cluster} for printing, plotting and the
single-partition helpers. Pick a different layer afterwards by indexing
\code{layers} directly; the fit does not need to be rerun.
}

\section{Reproducibility}{
EVoC is stochastic (neighbour search, graph partitioning and the embedding
all draw random numbers), so \code{seed} is a parameter rather than being taken
from R's RNG, as in \code{\link[=shoal_kmeans]{shoal_kmeans()}}. The same seed, parameters and data
give bitwise-identical results regardless of thread count, a stronger
guarantee than the reference implementation's, whose parallel stages can
race. Expect different seeds to give somewhat different clusterings; on
data EVoC suits, the structure they agree on is real.
}

\examples{
# Embedding-like data: directions with angular spread, not raw tabular data.
set.seed(1)
centres <- matrix(runif(6 * 48, -1, 1) * 0.6, nrow = 6)
x <- centres[rep(1:6, each = 140L), ] + matrix(rnorm(840 * 48, sd = 0.1), ncol = 48)

fit <- shoal_evoc(x, min_cluster_size = 15L)
fit
# every granularity remains available:
vapply(fit$layers, function(l) length(unique(l[!is.na(l)])), integer(1))

}
\references{
McInnes, L. (2023). \emph{EVoC: Embedding Vector Oriented Clustering}.
\url{https://github.com/TutteInstitute/evoc}
}
