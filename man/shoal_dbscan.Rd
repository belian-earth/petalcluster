% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbscan.R
\name{shoal_dbscan}
\alias{shoal_dbscan}
\title{DBSCAN Clustering}
\usage{
shoal_dbscan(x, eps = 0.5, min_samples = 5L, metric = c("euclidean", "cosine"))
}
\arguments{
\item{x}{A numeric matrix or data frame. Data frames are coerced to a matrix
using their numeric columns (non-numeric columns are dropped).}

\item{eps}{Neighbourhood radius. Default \code{0.5}.}

\item{min_samples}{Minimum number of points to form a dense region. Default \code{5L}.}

\item{metric}{Distance metric, one of \code{"euclidean"} or \code{"cosine"}.}
}
\value{
An object of class \code{c("shoal_dbscan", "shoal_clustering")}: a list
with components \code{cluster} (integer vector of cluster IDs, \code{NA} for noise),
\code{n_clusters}, \code{n_noise}, \code{data} (the input matrix), \code{algorithm}, \code{params},
and \code{metric}.
}
\description{
Density-based spatial clustering of applications with noise.
}
\examples{
res <- shoal_dbscan(as.matrix(iris[, 1:4]), eps = 0.5, min_samples = 5L)
res

}
\seealso{
\code{\link[=shoal_knn]{shoal_knn()}}, whose \code{plot()} method draws the k-nearest-neighbour
distance curve used to choose \code{eps}.
}
